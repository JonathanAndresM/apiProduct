TAREAS POS-9 Y POS-16

Se agregó una conexión a la base de datos local usando Sequelize.

Se agregó la carpeta 'sequelize_models' que contiene los modelos de la base de datos.

Se modificó la conexión para usar variables de entorno (.env) y se movió la lógica a la carpeta 'db'

Se modificó app.js para contener la conección inicial a la base de datos.
